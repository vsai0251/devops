# Git HUD

Git HUD is a lightweight dashboard and helper tool for Git operations.

## Features
- Branch, commit, and status overview
- Shortcut commands for common workflows
- Minimal dependencies

## Setup
1. Install dependencies: `npm install`
2. Run: `npm start`
